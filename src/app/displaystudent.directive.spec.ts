import { DisplaystudentDirective } from './displaystudent.directive';

describe('DisplaystudentDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplaystudentDirective();
    expect(directive).toBeTruthy();
  });
});
